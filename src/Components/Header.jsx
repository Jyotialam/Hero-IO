import React from 'react';
import NavBar from './NavBar';
import Banner from './Banner';

const Header = () => {
    return (
        <div>
            <NavBar/>
            
        </div>
    );
};

export default Header;